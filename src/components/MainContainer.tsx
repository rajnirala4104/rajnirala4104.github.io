import React, { Fragment } from "react";

const MainContainer: React.FC = () => {
   return (
      <Fragment>
         <div className="w-full grid place-content-center place-items-center bg-white"></div>
      </Fragment>
   );
};

export default MainContainer;
